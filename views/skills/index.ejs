<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express Skills</title>
</head>
<body>
    <h1>Skills</h1>
    <ul>
      <% skills.forEach(function(skill) { %>
        <li>
          <a href="/skills/<%= skills.id %>"><%= skill.skill %></a>
             aaaaand ...
          <%= skill.skill ? 'Mastered!' : 'not quite mastered' %>
        </li>
      <% }); %>
    </ul>
  </body>
</html>